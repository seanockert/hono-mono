{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:full": "cd .. && bun run build:client && cd server && rm -rf static && mkdir -p static && cp -r ../client/dist/* static/ && tsc",
    "build:prod": "cd .. && cross-env NODE_ENV=production bun run build:client && cd server && rm -rf static && mkdir -p static && cp -r ../client/dist/* static/ && tsc",
    "dev": "bun run --hot src/index.ts",
    "deploy": "bun run build:prod && wrangler deploy --minify",
    "deploy:dev": "bun run build:full && wrangler deploy --minify"
  },
  "dependencies": {
    "better-auth": "^1.3.9",
    "hono": "^4.9.7",
    "shared": "workspace:*",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
