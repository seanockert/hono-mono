{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bun run --hot src/index.ts",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "better-auth": "^1.3.7",
    "hono": "^4.9.4",
    "zod": "^4.1.0",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
