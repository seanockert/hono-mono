{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:full": "cd .. && bun run build:client && cd server && rm -rf static && mkdir -p static && cp -r ../client/dist/* static/ && bun run esbuild.config.js",
    "build:prod": "cd .. && cross-env NODE_ENV=production bun run build:client && cd server && rm -rf static && mkdir -p static && cp -r ../client/dist/* static/ && bun run esbuild.config.js",
    "dev": "bun run --hot src/index.ts",
    "deploy": "bun run build:prod && wrangler deploy",
    "deploy:dev": "bun run build:full && wrangler deploy"
  },
  "dependencies": {
    "better-auth": "^1.4.17",
    "hono": "^4.11.7",
    "kysely": "^0.28.10",
    "kysely-d1": "^0.4.0",
    "shared": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.24.2"
  }
}
