{
  "name": "hono-auth",
  "version": "0.0.1",
  "description": "A monorepo with Bun, Hono, Vue, and Better Auth",
  "keywords": [
    "better-auth",
    "bun",
    "hono",
    "monorepo",
    "vite",
    "vue"
  ],
  "homepage": "",
  "license": "MIT",
  "author": "Sean Ockert",
  "workspaces": [
    "./server",
    "./client",
    "./shared"
  ],
  "scripts": {
    "dev": "concurrently \"cd shared && bun run dev\" \"cd server && bun run dev\" \"cd client && bun run dev\"",
    "build": "cd shared && bun run build && cd ../server && bun run build && cd ../client && bun run build",
    "deploy": "cd server && bun run deploy && cd ../client && bun run deploy",
    "postinstall": "cd shared && bun run build && cd ../server && bun run build",
    "lint": "oxlint --ignore-path .gitignore .",
    "format": "oxfmt --ignore-path .gitignore .",
    "format:check": "oxfmt --ignore-path .gitignore --check ."
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20260307.1",
    "@hono/zod-validator": "^0.7.6",
    "better-auth": "^1.5.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "bun-types": "latest",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
